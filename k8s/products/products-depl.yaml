apiVersion: apps/v1
kind: Deployment
metadata:
  name: products-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: products
  template:
    metadata:
      labels:
        app: products
    spec:
      containers:
        - name: products
          image: cstudent/products
          env:
            - name: PGDATABASE
              value: "payments-database"
            - name: PGHOST
              value: products-postgres-cluster-ip
            - name: PGUSER
              value: "products"
            - name: PGPASSWORD
              value: "jeff"
            - name: JWT_KEY
              value: "jeff"
              # Kafka has to go here
